figure;

subplot(1,3,1);
show(myOccMap);
title('map sans odometrie');

subplot(1,3,2);
show(myOccMap_avec_odoms);
title('map avec odometrie');

subplot(1,3,3);
show(myOccMap_lc_rad4);
title('map avec lc radius=4');

subplot(1,3,3);
show(myOccMap_lc_rad4);
title('map avec lc radius=4');